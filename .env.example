# Backend Configuration
PORT=3000
CORS_ORIGIN=*
HOST=0.0.0.0

# Optional: remote ETL inputs (fallback to local JSON when empty)
# Recommend hosting on GitHub Raw, S3+CloudFront, GCS, Azure Blob, Netlify/Cloudflare Pages
ETL_TABLE_REPORT_URL=""
ETL_MONTHLY_STATEMENTS_URL=""

# Local development (backend running on host)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kudwa_db"

# When using docker-compose, backend connects to Postgres service "db"
# DATABASE_URL="postgresql://postgres:postgres@db:5432/kudwa_db"

# (Optional) docker-compose Postgres variables
# POSTGRES_DB=kudwa_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres



# Frontend Configuration
VITE_API_URL="http://localhost:3000"
# For Docker build of the frontend, the image uses VITE_API_URL=/api and Nginx proxies /api -> backend


# Common
NODE_ENV="development"
