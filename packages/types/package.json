{
  "name": "@kudwa-code-challenge/types",
  "private": true,
  "version": "0.1.2",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --out-dir dist --clean",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist && rm -rf ../../node_modules/.tmp/tsconfig.types.tsbuildinfo"
  },
  "dependencies": {
    "zod": "^4"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5"
  }
}
